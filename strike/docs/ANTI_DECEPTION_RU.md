# 🍯 Anti-Deception & Honeypot Detection

> **Как SENTINEL Strike обнаруживает ловушки и ложноположительные срабатывания**

---

## Обзор

SENTINEL Strike включает **AI Adaptive Engine** — систему обнаружения honeypot'ов, tarpit'ов и deception-технологий. Это критически важно для:

- ✅ Исключения ложноположительных срабатываний
- ✅ Защиты атакующего от обнаружения
- ✅ Экономии времени на нерелевантных целях
- ✅ Точных отчётов без "фейковых" уязвимостей

---

## Уровни угрозы (ThreatLevel)

Strike автоматически классифицирует цель по 5 уровням:

| Уровень           | Индикатор                     | Действие               |
| ----------------- | ----------------------------- | ---------------------- |
| 🟢 **NORMAL**     | Обычная цель                  | Продолжить атаку       |
| 🟡 **SUSPICIOUS** | Подозрительные паттерны       | Увеличить delay 3x     |
| 🔴 **HONEYPOT**   | Высокая вероятность ловушки   | Рекомендуется abort    |
| 🐢 **TARPIT**     | Намеренное замедление         | Минимальные запросы 5x |
| 🎭 **DECEPTION**  | Активная deception-технология | Abort + не репортить   |

---

## Индикаторы обнаружения

### 📊 Статистические аномалии

| Индикатор              | Вес | Описание                       |
| ---------------------- | --- | ------------------------------ |
| **Fast Responses**     | 30% | > 50% ответов < 10ms           |
| **High Bypass Rate**   | 25% | > 70% bypass rate              |
| **Uniform Techniques** | 20% | Все техники работают одинаково |
| **Consistent Timing**  | 15% | Низкая дисперсия response time |
| **All Critical**       | 10% | Всё помечено как "CRITICAL"    |

### 🔍 Как это работает

```
1. ResponseAnalyzer собирает метрики за последние 100 запросов
2. HoneypotDetector вычисляет weighted score (0.0 - 1.0)
3. Если score > 0.7 → высокая вероятность honeypot
4. StrategyAdapter адаптирует стратегию (замедление/abort)
5. В отчёте findings помечаются как "suspicious"
```

---

## Примеры обнаружения

### 🔴 Honeypot детекция

```
⚠️ SENTINEL Strike detected honeypot indicators:

Honeypot Score: 0.82 (HIGH)

Breakdown:
  • fast_responses: 0.90 (45 of 50 responses < 10ms)
  • high_bypass_rate: 0.75 (85% bypass rate - too good to be true)
  • uniform_techniques: 0.80 (SQLi, XSS, LFI all work equally)
  • consistent_timing: 0.60 (avg response 5ms, variance 2ms)

🍯 RECOMMENDATION: This is likely a honeypot.
   Do NOT report these as real vulnerabilities.
   Consider aborting scan.
```

### 🟡 Suspicious паттерны

```
⚠️ SENTINEL Strike detected suspicious patterns:

Threat Level: SUSPICIOUS (confidence: 0.55)

Anomalies:
  • Abnormally high bypass rate: 72%
  • Too many fast responses (<10ms): 35%

🔍 RECOMMENDATION:
   • Verify findings manually before reporting
   • Increase delay between requests
   • Switch to cautious mode
```

---

## Настройка в Web Console

В правой панели дашборда (Stats & AI):

| Опция                      | Описание                                |
| -------------------------- | --------------------------------------- |
| **AI Adaptive**            | Включить/выключить адаптивный режим     |
| **Analysis Interval**      | Анализ каждые N запросов (default: 20)  |
| **Auto-Abort on Honeypot** | Автоматически прерывать при обнаружении |

---

## Настройка в CLI

```bash
# Включить AI Adaptive (по умолчанию ON)
python -m strike -t https://example.com --ai-adaptive

# Выключить (для полного сканирования несмотря на подозрения)
python -m strike -t https://example.com --no-ai-adaptive

# Установить интервал анализа
python -m strike -t https://example.com --analysis-interval 10
```

---

## API использование

```python
from strike.ai.ai_adaptive import AIAdaptiveEngine, ThreatLevel

# Инициализация
engine = AIAdaptiveEngine(
    gemini_key="AIza...",  # Для AI-анализа (опционально)
    analysis_interval=20,
    enabled=True
)

# Записать ответ
engine.record_response(
    response_time_ms=5.2,
    status_code=200,
    content_length=1500,
    is_bypass=True,
    payload_type="sqli",
    technique="UNION-based"
)

# Проверить уровень угрозы
threat = engine.get_threat_level()
if threat == ThreatLevel.HONEYPOT:
    print("🍯 Honeypot detected! Aborting...")

# Получить рекомендованный delay
delay = engine.get_adjusted_delay(base_delay=500)  # Может вернуть 1500 при SUSPICIOUS

# Проверить нужно ли продолжать
if not engine.should_continue():
    print("AI recommends to abort")
```

---

## Интеграция с отчётами

При обнаружении подозрительных паттернов, отчёт содержит:

### ⚠️ Honeypot Warning секция

```html
🍯 Обнаружены подозрительные ответы 23 из 150 bypasses имеют аномально быстрое
время ответа (<10ms). Это может указывать на: • Honeypot/Tarpit — фиктивные
уязвимости для отслеживания атакующих • Deception Technology — системы
обнаружения и замедления атак • WAF с фейковыми сигнатурами — намеренно
пропущенные запросы для анализа ⚠️ Рекомендация: Эти findings требуют особо
тщательной ручной верификации.
```

### 📊 False Positive Rates

В отчёте указываются типичные FPR для каждого типа уязвимости:

| Тип                | FPR    | Причина                                      |
| ------------------ | ------ | -------------------------------------------- |
| WAF Bypass         | 20-30% | WAF может пропускать без реальной уязвимости |
| SQL Injection      | 5-10%  | Response может измениться по другим причинам |
| XSS                | 15-20% | Payload может отразиться, но не выполниться  |
| LFI/Path Traversal | 10-15% | Файл может не существовать                   |

---

## Best Practices

### ✅ Рекомендации

1. **Всегда проверяйте findings вручную**

   - Используйте предоставленные PoC
   - Проверьте в Burp Suite / браузере

2. **Обращайте внимание на AI Warnings**

   - Если Strike предупреждает о honeypot — это серьёзно
   - Не включайте подозрительные findings в отчёты

3. **Используйте Gemini для глубокого анализа**

   - С API ключом Strike использует Gemini для анализа паттернов
   - Более точное обнаружение deception-технологий

4. **Проверяйте response time distribution**
   - Реальные системы имеют variance в response time
   - Константные 5-10ms — красный флаг

### ⚠️ Что делать при обнаружении Honeypot

1. **НЕ репортите** эти findings как реальные уязвимости
2. **Задокументируйте** наличие honeypot в отчёте
3. **Уведомите заказчика** что обнаружена deception-технология
4. **Переключитесь** на другие цели/эндпоинты

---

## Техническая архитектура

```
┌─────────────────────────────────────────────────────┐
│              AI Adaptive Engine                      │
├─────────────────────────────────────────────────────┤
│  ┌───────────────┐  ┌───────────────────────────┐   │
│  │ Response      │  │ Honeypot Detector         │   │
│  │ Analyzer      │──│ • Statistical analysis    │   │
│  │ (Window: 100) │  │ • Gemini AI (optional)    │   │
│  └───────────────┘  │ • Score calculation       │   │
│                     └───────────────────────────┘   │
│                              │                       │
│                              ▼                       │
│                     ┌───────────────────────────┐   │
│                     │ Strategy Adapter          │   │
│                     │ • Delay multiplier        │   │
│                     │ • Technique selection     │   │
│                     │ • Abort decision          │   │
│                     └───────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

_SENTINEL Strike v3.0 — Smart enough to know when NOT to attack_
