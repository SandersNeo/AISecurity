# Глубокий архитектурный анализ: RLM MCP Server

**Дата:** 2026-01-19  
**Цель:** Выявить ВСЕ возможные проблемы до начала реализации

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (блокеры)

### C1: InfiniRetri НЕ РАБОТАЕТ с облачными API

```
InfiniRetri требует: attention weights
OpenAI API возвращает: только текст
Anthropic API возвращает: только текст
Google API возвращает: только текст
```

**Факт:** InfiniRetri работает ТОЛЬКО с локальными моделями (Ollama, llama.cpp)

**Влияние:** 90% пользователей используют облачные API

**Решения:**
1. Hybrid: локальная маленькая модель для retrieval, облачная для генерации
2. Переключиться на embedding-based retrieval (но это RAG, не InfiniRetri)
3. Ждать когда API начнут возвращать attention (маловероятно)

---

### C2: C³ — это PoC, не production code

```
poc_context_crystal_v4.py = 200 строк
Работает на: простом тестовом документе
НЕ тестировался на: реальных проектах
```

**До продакшена:**
- [ ] Интеграция с spaCy/transformers для NER
- [ ] Multi-file support
- [ ] Инкрементальное обновление при изменении файлов  
- [ ] Бенчмарки на реальных проектах
- [ ] Edge cases (unicode, binary files, huge files)

**Оценка работы:** 2-4 недели дополнительной разработки

---

### C3: H-MEM консолидация — кем делается?

```
Консолидация требует LLM вызов
"Сожми эту историю диалога в ключевые факты"
```

**Варианты:**
| Модель | Плюсы | Минусы |
|--------|-------|--------|
| Cloud (GPT-4) | Качественно | Стоит денег |
| Local (Llama 7B) | Бесплатно | Может быть некачественно |
| Без LLM (heuristics) | Бесплатно | Примитивно |

**Скрытая стоимость:** Каждая консолидация = N токенов

---

### C4: Первичная индексация большого проекта

```
Проект: 500K токенов
Первый запуск RLM:
1. Загрузить все файлы
2. Построить C³ crystal
3. Проиндексировать для InfiniRetri

Время: ??? (не измерено)
```

**Риск:** Первый запуск может занять 10-30 минут

---

## 🟡 СРЕДНИЕ ПРОБЛЕМЫ

### M1: Обновление при изменении кода

```
Вася: *изменил файл user.py*
RLM: *должен обновить crystal*

Как? Полное перестроение или инкрементальное?
```

**Не реализовано:** Инкрементальное обновление

---

### M2: Multi-file projects

```
C³ PoC работает с: одной строкой текста
Реальный проект: 217 файлов

Как объединять? Один crystal на все файлы? Crystal per file?
```

**Не спроектировано**

---

### M3: Конфликт между C³ и H-MEM

```
C³: хранит факты о КОДЕ
H-MEM: хранит факты о ДИАЛОГЕ

Как они взаимодействуют?
Какой приоритет при запросе?
```

**Не спроектировано**

---

### M4: Какой LLM делает что?

```
Задача              | Какая модель?
--------------------|------------------
Консолидация H-MEM  | ?
Retrieval InfiniRetri | ?
Построение C³       | ? или без LLM?
Ответ на запрос     | Пользовательская
```

**Не определено**

---

### M5: Fallback при ошибках

```
InfiniRetri не нашёл релевантный контекст → ?
C³ не извлёк нужный примитив → ?
H-MEM пустой → ?
```

**Не определено**

---

## 🟢 МЕЛКИЕ ПРОБЛЕМЫ

### L1: Установка требует Python
### L2: Конфигурация через JSON
### L3: Нет GUI
### L4: Логирование примитивное
### L5: Нет health check endpoint

---

## 📊 Матрица рисков

| ID | Влияние | Вероятность | Итог |
|----|---------|-------------|------|
| C1 | 🔴 High | ✓ Факт | БЛОКЕР |
| C2 | 🔴 High | ✓ Факт | БЛОКЕР |
| C3 | 🟡 Medium | 🟡 Medium | Планировать |
| C4 | 🟡 Medium | 🔴 High | Планировать |
| M1-M5 | 🟡 Medium | 🟡 Medium | Планировать |

---

## ❓ ОТКРЫТЫЕ ВОПРОСЫ

1. **InfiniRetri + Cloud:** Как решаем? Hybrid или отказываемся для cloud?
2. **C³ scope:** Сколько времени на доведение до production?
3. **Консолидации:** Какую модель используем?
4. **Multi-file:** Один crystal или много?
5. **Приоритет компонентов:** C³ vs H-MEM vs InfiniRetri — что первое?

---

## 🎯 РЕКОМЕНДАЦИЯ

Перед началом реализации MCP Server нужно:

1. **Решить C1:** Выбрать стратегию для InfiniRetri + Cloud
2. **Решить C2:** Довести C³ до production (2-4 недели)
3. **Спроектировать M2-M5:** Multi-file, взаимодействие компонентов

**Иначе:** Построим каркас, который придётся переделывать

---

*Статус: ТРЕБУЕТ РЕШЕНИЯ ПО БЛОКЕРАМ*
